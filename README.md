# record

`github.com/dal-go/record` provides persistence-neutral record identity,
envelopes, mapping helpers, and declarative change commands. It deliberately
does not expose database, session, query, or transaction capabilities.
